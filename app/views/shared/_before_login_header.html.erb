<div class="fixed top-0 z-50 flex h-16 w-full items-center pl-1 shadow bg-opacity-90">
  <nav class="navbar w-full">
    <div class="flex justify-between w-full">
      <div class="flex items-center">
        <%= link_to "/", class: "btn btn-ghost flex items-center space-x-4" do %>
          <%= image_tag "logo.png", class: "w-10 object-contain" %>
          <h1 class="md:text-xl font-semibold">All Ready</h1>
        <% end %>
        <p class="md:px-3.5 text-sm">かばんの中身を共有するアプリ</p>
      </div>
      <div class="md:flex space-x-4 mr-4 font-semibold">
        <%= link_to new_user_session_path, class: "btn btn-primary text-sm px-5 flex items-center" do %>
          <span class="material-symbols-outlined mr-2">login</span>
          ログイン
        <% end %>
      </div>
    </div>
  </nav>
</div>
